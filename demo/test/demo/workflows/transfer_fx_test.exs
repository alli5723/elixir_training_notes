defmodule Demo.Workflows.TransferFxTest do
  use ExUnit.Case
  doctest Demo.Workflows.TransferFx

  # test "Worklow sends money in the correct value" do

  # end
  test "1 + 1" do
    sum = 1 + 1
    assert sum = 2
  end
end
